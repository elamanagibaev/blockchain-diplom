SECRET_KEY=CHANGE_ME_SECRET
ACCESS_TOKEN_EXPIRE_MINUTES=240

POSTGRES_SERVER=db
POSTGRES_USER=app
POSTGRES_PASSWORD=app
POSTGRES_DB=file_registry

FILE_STORAGE_BACKEND=local
FILE_STORAGE_PATH=/data/files

# Blockchain (optional for first run, but required for /api/blockchain/*)
WEB3_PROVIDER_URL=http://blockchain:8545
# Leave empty in docker-compose local flow: backend reads /shared/contract_address.txt
CONTRACT_ADDRESS=
CONTRACT_ADDRESS_FILE=/shared/contract_address.txt
CONTRACT_OWNER_PRIVATE_KEY=0xYOUR_PRIVATE_KEY
CONTRACT_OWNER_ADDRESS=0xYOUR_ADDRESS
CHAIN_ID=31337

# Ссылки на транзакции в ответах API (без завершающего /)
BLOCK_EXPLORER_URL=http://localhost:4000

BACKEND_CORS_ORIGINS=["http://localhost:5173"]

