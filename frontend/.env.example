VITE_API_BASE_URL=http://localhost:8000/api

# База обозревателя для ссылок на tx (без /), если не задана в ответе API (BLOCK_EXPLORER_URL на бэкенде)
VITE_BLOCK_EXPLORER_URL=http://localhost:4000

# Журнал /explorer: true = статические моки вместо GET /admin/journal/document-events и /blockchain/events
# VITE_EXPLORER_USE_MOCK=true
